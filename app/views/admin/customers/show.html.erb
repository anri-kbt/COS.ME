<div class="container">
  <div class="row">
    <div class="title">
      <span><%=@customer.nickname%>さんの会員詳細</span>
    </div>
  </div>
  <div class="row">
    <table class="table table-borderless mb-5">
      <tbody>
        <tr>
          <td width="50vw">会員ID</td>
          <td width="50vw"><%= @customer.id %></td>
        </tr>
        <tr>
          <td>ニックネーム</td>
          <td><%= @customer.nickname %></td>
        </tr>
        <tr>
          <td>ユーザーID</td>
          <td><%= @customer.user_id %></td>
        </tr>
        <tr>
          <td>電話番号</td>
          <td><%= @customer.call_number %></td>
        </tr>
        <tr>
          <td>メールアドレス</td>
          <td><%= @customer.email %></td>
        </tr>
        <tr>
          <td>会員ステータス</td>
            <% if @customer.is_deleted==false %>
              <td class="text-success font-weight-bold">有効</td>
            <% else %>
              <td class="text-muted font-weight-bold">退会</td>
            <% end %>
        </tr>
        <tr>
          <td></td>
          <td>
            <%= link_to"編集する",edit_admin_customer_path(@customer.id),class:"btn btn-success mr-5" %>
          </td>
        </tr>
      </tbody>
    </table>
  </div>
</div>


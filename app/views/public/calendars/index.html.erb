<table>
  <p><%= @today.strftime("< %m / %Y >") %></p>
  <% @calendar_data.each do |date| %>
  <tr>
    <td>
      <%= date.day %><%= day_name %>
    </td>
  </tr>
  <% end %>
  <% end %>
</table>


<% t('date.abbr_day_names').each do |day_name| %>
      <td>
        <%= day_name %>
      </td>
    <% end %>